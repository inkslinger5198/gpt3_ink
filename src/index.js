import React from 'react';
import ReactDOM from 'react-dom';

import App from './App.js';
import './index.css';

ReactDOM.render(<App />, document.getElementById('root'));
//This will render the App component (which is the parent component) to the root element in the DOM., this means the application will be hooked to index.html